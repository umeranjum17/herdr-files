id = "muxr.code"
name = "Files & history"
version = "0.1.0"
min_herdr_version = "0.8.0"
platforms = ["linux", "macos"]
description = "Browse the files and git history of the repository you are working in, on your phone"
